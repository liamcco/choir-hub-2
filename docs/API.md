Api is selfdocumenting using Hono OpenAPI
Remember: DescribeResponse for GET requests
DescribeRoute for everything else
---> Better type handling

scalar and swagger docs are served if you are authenticated

Put down zod-schema ONCE and typescript will make sure you send back the right thing