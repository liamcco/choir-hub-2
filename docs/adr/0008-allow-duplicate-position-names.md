# Allow duplicate position names

CSK Choir Hub will treat `Position.name` as display text rather than a globally unique key. Different choirs or scopes may each have a position called Concert Master, while one shared role across multiple groups remains a single Position with multiple Position Scopes.
